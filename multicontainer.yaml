apiVersion: v1
kind: Pod
metadata:
  name: multicontainer
spec:
  containers:
  - name: myapp
    image: <Image>
    command:
      - sleep
      - "3600"
  - name: nginx
    image: nginx

